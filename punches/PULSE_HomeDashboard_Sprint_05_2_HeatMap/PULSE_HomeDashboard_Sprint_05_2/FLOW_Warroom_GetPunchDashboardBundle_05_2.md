# Flow — Sprint 05.2

No cambia la firma pública.

Entrada: `ProjectId`, `TemplateId`.

Ejecutar `warroom.usp_GetPunchDashboardBundle` y devolver la columna `result`.

`matrix[]` añade `Intensity` e `IntensityBand`. No añadir Parse JSON dentro del Flow.
