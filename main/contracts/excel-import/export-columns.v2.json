{
  "contract": "PULSE.PunchExcelExportColumns",
  "version": 2,
  "flow": "Warroom_ExportPunchesToExcel",
  "compatibility": {
    "previousVersion": 1,
    "classification": "breaking",
    "reason": "Aligns the logical import contract with the identifiers and checksum emitted by the real export."
  },
  "physicalColumns": [
    { "columnKey": "PunchExportLogId", "logicalField": "ExportBatchId", "type": "integer", "nullable": false, "editable": false, "required": true, "example": 4812, "validation": "Must reference the stored export log/batch." },
    { "columnKey": "ProjectId", "logicalField": "ProjectId", "type": "integer", "nullable": false, "editable": false, "required": true, "example": 4049, "validation": "One value per file; must match the stored export." },
    { "columnKey": "TemplateId", "logicalField": "TemplateId", "type": "integer", "nullable": false, "editable": false, "required": true, "example": 12, "validation": "One value per file; must match the exported punch and stored export." },
    { "columnKey": "PunchId", "logicalField": "WorkItemId", "type": "integer", "nullable": false, "editable": false, "required": true, "example": 100234, "validation": "Unique in file and present in the stored export." },
    { "columnKey": "RowHash", "logicalField": "ExportRowChecksum", "type": "string", "nullable": false, "editable": false, "required": true, "maxLength": 64, "example": "A1B2C3...", "validation": "Uppercase SHA-256 hexadecimal generated by SQL." },
    { "columnKey": "Original Row Hash", "sourceField": "OriginalRowHash", "logicalField": "OriginalRowChecksum", "type": "string", "nullable": false, "editable": false, "required": true, "maxLength": 64, "example": "A1B2C3...", "validation": "Immutable copy of RowHash created by the Office Script." }
  ],
  "exportInformationFields": [
    { "field": "ExportedOnUtc", "logicalField": "ExportedAtUtc", "type": "datetime", "nullable": false, "example": "2026-07-28T14:23:03Z", "validation": "ISO 8601 UTC; stored in the hidden Export Information sheet." }
  ],
  "concurrency": {
    "mechanism": "checksum",
    "rowVersionColumnPresent": false,
    "rule": "Recompute the canonical current-row checksum and compare it with OriginalRowHash.",
    "knownGap": "The supplied SQL RowHash covers identifiers and dynamic custom fields, not every editable standard field."
  },
  "selection": {
    "technicalColumnsAreOwnedByOfficeScript": true,
    "powerAppsMustNotAppendTechnicalColumns": true
  }
}
