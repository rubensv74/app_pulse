# Solution component inventory — 1.0.0.2

Solution `baseline_pulse` / `BASELINE_PULSE`, version `1.0.0.2`, publisher `pulse` / `PULSE`, Unmanaged (`Managed=0`). `solution.xml` declares 70 root components: 69 type-29 workflows and one type-300 Canvas App (`new_pulse_9584c`). All 69 workflow root components have corresponding physical JSON files. `MissingDependencies` is empty.

No connection reference, environment variable, custom connector, Dataverse table, plugin, security role, custom API, model-driven app, Office Script or service endpoint is a root component. Runtime connector/dependency references remain embedded in app/flow metadata and are evaluated separately.
