# CREATE_POWER_APPS_INCREMENT

Construye **[INCREMENTO]** pequeño y verificable. Si toca UI aplica PDS, Premium Screen Standard, Page Header Hierarchy y `COMPONENT_CATALOG.md`; usa `REUSE → EXTEND_SHARED → CREATE_SHARED → LOCAL_ONLY`. Aplica Async Action Guard, evita globals innecesarios y define prueba concreta en Studio.