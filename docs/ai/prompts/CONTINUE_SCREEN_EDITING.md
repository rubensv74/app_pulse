# CONTINUE_SCREEN_EDITING

Continúa con **[PANTALLA]**. Lee `AGENTS.md`, PDS, Premium Screen Standard, Page Header Hierarchy y contratos/gates de la pantalla. Revisa `COMPONENT_CATALOG.md` y la implementación real antes de tocar UI. Clasifica cada necesidad como `REUSE / EXTEND_SHARED / CREATE_SHARED / LOCAL_ONLY`. Conserva lo validado, corrige por incrementos y no declares Visual Gate sin Studio.