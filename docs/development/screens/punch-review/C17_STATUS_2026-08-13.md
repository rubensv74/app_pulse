# C17 — Estado de validación en Studio — 2026-08-13

## Estado observado

La última captura de Power Apps Studio confirma visualmente:

- estructura `Review Queue | Main Workspace | Right Rail` aprobada;
- main workspace claramente dominante;
- Review Progress arriba del rail derecho;
- Session Activity debajo;
- rail derecho sin clipping;
- Comments y Custom Fields en dos columnas bajo Punch Overview;
- Punch template heredado correctamente desde Home;
- Queue Scope retirado del header visible;
- Custom Fields con controles modernos para Choice/MultiChoice;
- opciones reales visibles en los ComboBox;
- Number visualmente alineado con los inputs Outline;
- footer de Comments y Custom Fields visible.

## Defecto UX residual identificado

El comportamiento del botón Back ya utiliza navegación contextual mediante `Back()`, pero su label histórico quedó fijado a `Back to Punches`.

Cuando la sesión procede de Home, el texto debería describir Home. Cuando procede de Punch List, debería describir Punch List. Esto es un ajuste de microcopy/contexto; no requiere modificar la navegación.

## Source sync pendiente

El archivo `power-apps/screens/PunchReview/scr_PunchReview.pa.yaml` del repositorio todavía representa una versión anterior a varios cambios aplicados manualmente y validados en Studio.

Antes del cleanup destructivo C17-E se requiere volver a sincronizar el Source Code actual de Studio con el repositorio.

No eliminar controles ni limpiar propiedades estructurales tomando el archivo repository-owned actual como única fuente de verdad hasta completar esa sincronización.

## Estado por bloque

```text
C17-A             VISUAL PASS
C17-B             VISUAL PASS
C17-C             VISUAL PASS
C17-C-FIX1        VISUAL PASS
C17-D             VISUAL PASS — pendiente smoke test completo Save/Reload/scroll
C17-FIX2          VISUAL PASS
C17-E             READY — bloqueado para cleanup destructivo por SOURCE SYNC
```

## Siguiente gate

1. sincronizar Source Code actual de `scr_PunchReview`;
2. ejecutar cleanup estructural;
3. ejecutar matriz responsive y funcional C17-E;
4. cerrar C17;
5. reanudar DF-07B.